version: 2.1
jobs:
  build:
    docker:
      - image: gcr.io/google-containers/busybox:1.24
    steps:
      - run:
          name: hello
          command: echo 'hello'

workflows:
  branch-build:
    jobs:
      - build:
          filters:
            branches:
              only:
                - main
