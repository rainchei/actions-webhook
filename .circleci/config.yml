version: 2.1
jobs:
  test:
    docker:
      - image: busybox
    steps:
      - run:
          name: hello
          command: echo 'hello'
      - run:
          name: test
          command: |
            echo 'test'
  build:
    docker:
      - image: curlimages/curl
    steps:
      - run:
          name: action webhook
          command: |
            cat \<< EOF_A > /tmp/tmp-message.json
            {
              "event_type": "${CIRCLE_BRANCH}"
            }
            EOF_A
            JSON="$(cat /tmp/tmp-message.json)"
            curl -H "Authorization: token ${PAT}" -d "${JSON}" https://api.github.com/repos/rainchei/actions-webhook/dispatches

workflows:
  test-and-build:
    jobs:
      - test:
          filters:
            branches:
              only:
                - main
                - dev
      - build:
          context: actions-webhook
          requires:
            - test

